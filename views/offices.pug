extends layout-admin

block content
  h3= title

  div
    table
      thead
        th(align='left') City
        th(align='center') Phone
      tbody
        each row, i in rows
          tr
            td(align='left')
              h5
                a(href=row.href) #{row.city}
            td
              h6
                span #{row.phone}